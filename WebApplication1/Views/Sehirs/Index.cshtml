@model IEnumerable<Project_Model.Sehir>
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    ViewData["Title"] = "Index";
    Layout = "_Layout";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SehirAdı)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SehirAdı)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SehirID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SehirID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SehirID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
